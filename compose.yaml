services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: orders_incremental
      POSTGRES_USER: orders
      POSTGRES_PASSWORD: orders
    ports:
      - "127.0.0.1:5434:5432"
    volumes:
      - incremental-orders:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U orders -d orders_incremental"]
      interval: 5s
      timeout: 3s
      retries: 10
volumes:
  incremental-orders:
